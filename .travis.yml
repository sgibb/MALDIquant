language: r
warnings_are_errors: true

r_github_packages:
  - jimhester/covr

r_binary_packages:
  - testthat
  - knitr

after_success:
  - Rscript -e 'library(covr);coveralls()'

notifications:
  email:
    on_success: change
    on_failure: change
