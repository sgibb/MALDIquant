language: r
cache: packages
warnings_are_errors: true
r_binary_packages:
- covr
after_success:
- Rscript -e 'library(covr);codecov(exclusions="R/Deprecated.R")'
notifications:
  email:
    on_success: change
    on_failure: change
r:
  - oldrel
  - release
  - devel
